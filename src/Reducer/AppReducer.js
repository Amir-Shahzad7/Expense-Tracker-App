const AppReducer = () => {
    return (
        (state, action) => {
            switch(action) {
                default:
                    return state;
            }
        }
    )
}

export default AppReducer;
